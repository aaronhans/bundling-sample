var $ = require('jquery');

var helpers = require('./helpers');
var mlsMemberShipsHB = require('./_mls_memberships.handlebars');
var data = require('./data.json');

var accountSettings = {
	init: function(zUser, cb) {
		this.cacheElements();
	},

	cacheElements: function() {
		console.log(data);
	  var html = mlsMemberShipsHB(data);
		console.log(html);
		document.getElementById('mls-memberships-list').innerHTML = html;
	}
}

module.exports = accountSettings;
